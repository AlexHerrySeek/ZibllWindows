<ui:FluentWindow x:Class="ZibllWindows.RemoveKeysPage"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Xóa Product Key"
                 Width="500"
                 Height="300"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Xóa Product Key" 
                       FontSize="20" 
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBlock Text="Gỡ bỏ Product Key đã cài đặt"
                       FontSize="12"
                       Foreground="Gray"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="ui:Button">
                    <Setter Property="Margin" Value="0,0,0,15"/>
                </Style>
            </StackPanel.Resources>
            <ui:Button x:Name="BtnRemoveWindowsKey"
                       Content="🗑️ Xóa Key Windows"
                       Click="BtnRemoveWindowsKey_Click"
                       HorizontalAlignment="Stretch"
                       Padding="15,12"
                       FontSize="14"
                       Appearance="Danger"/>
            
            <ui:Button x:Name="BtnRemoveOfficeKeys"
                       Content="🗑️ Xóa Tất Cả Key Office"
                       Click="BtnRemoveOfficeKeys_Click"
                       HorizontalAlignment="Stretch"
                       Padding="15,12"
                       FontSize="14"
                       Appearance="Danger"/>
        </StackPanel>
        
        <TextBlock x:Name="LblStatus"
                   Grid.Row="2"
                   Text="Sẵn sàng..."
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,20,0,0"
                   TextWrapping="Wrap"/>
    </Grid>
</ui:FluentWindow>
